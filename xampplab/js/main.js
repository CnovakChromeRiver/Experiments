require.config({
	paths: {
		jquery: 'vendor/jquery/jquery',
		text: 'vendor/require/text',
		handlebars: 'vendor/handlebars/base',
		zoomooz: 'plugin/zoomooz'
	}
});

define(['app'], function(app) {
	// run app
});